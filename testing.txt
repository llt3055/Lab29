Lab 30: Alpha Release Testing Document
Author: Tianyi Cao
Project: Registration Simulation

--- Test Case 1: File Input/Output Operations ---
Description: Verify that the program correctly opens, reads, and parses "students_data.txt".
Expected Result: The program should output "Successfully loaded [X] student records into waitlists." without crashing.
Actual Result: PASS. The program successfully reads the CSV format and populates the Waitlist arrays.

 --- Test Case 2: Drop Simulation Logic (10% Probability) ---
Description: Verify that currently enrolled students have an approximate 10% chance of dropping the course in each time period.
Expected Result: The number of students in the [Dropped] category should gradually increase over the 72-hour loop.
Actual Result: PASS. The console logs confirm that students are successfully shifted from Enrolled to Dropped.

--- Test Case 3: Waitlist Automation Logic ---
Description: Verify that waitlisted students are automatically moved into the course when the enrolled list is under the capacity of 10.
Expected Result: The [Waitlisted] count should decrease, and the [Enrolled] count should increase until capacity is reached.
Actual Result: PASS. Waitlisted students successfully fill the empty spots left by dropped students.

```cpp
void test_loadData_unit() {
    cout << "\n--- Starting Unit Test: loadData() ---" << endl;
    
    // Initialize simulation with a dummy period just for testing
    RegistrationSimulation testSim(1); 

    // Test Phase 1: Ensure the program doesn't crash on invalid input
    cout << "[Test Phase 1] Testing with an invalid file name:" << endl;
    testSim.loadData("fake_file_that_does_not_exist.txt");
    // Expected Output: "Error: Could not open file..."

    // Test Phase 2: Ensure the program correctly parses the real data
    cout << "\n[Test Phase 2] Testing with a valid file:" << endl;
    testSim.loadData("students_data.txt");
    // Expected Output: "Successfully loaded [X] student records..."
    
    cout << "--- Unit Test Complete ---" << endl;
}

```cpp
void test_loadData_unit() {
    cout << "\n--- Starting Unit Test: loadData() ---" << endl;
    
    // Initialize simulation with a dummy period just for testing
    RegistrationSimulation testSim(1); 

    // Test Phase 1: Ensure the program doesn't crash on invalid input
    cout << "[Test Phase 1] Testing with an invalid file name:" << endl;
    testSim.loadData("fake_file_that_does_not_exist.txt");
    // Expected Output: "Error: Could not open file..."

    // Test Phase 2: Ensure the program correctly parses the real data
    cout << "\n[Test Phase 2] Testing with a valid file:" << endl;
    testSim.loadData("students_data.txt");
    // Expected Output: "Successfully loaded [X] student records..."
    
    cout << "--- Unit Test Complete ---" << endl;
}

// To execute this unit test, the main() function would be temporarily replaced with:
/*
int main() {
    test_loadData_unit();
    return 0;
}
*/